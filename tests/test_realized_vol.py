"""
Tests for each RV estimator. Verifies: standard RV on constant returns gives zero,
bipower variation matches known results, estimators handle edge cases (single
observation, zero returns). Catches subtle math bugs that are easy to introduce.
"""
